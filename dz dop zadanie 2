#include <iostream> 
using namespace std; 
int main() 
{ 
int b; 
int n; 
n=9; 
int mas[n]; 
for (int i=0; i < n; i++) 
{ 
cin » mas[i]; 
} 
cout « "Kybi prostih 4isel: "; 
for (int i=0; i < n; i++) 
{ 
for (int a=2; a <= mas[i]; a++) 
{ 
if (mas[i]%a == 0 && mas[i] != a) 
{ 
mas[i] = 0; 
break; 
} 
} 
if (mas[i] != 0) 
{ 
b=0; 
b=mas[i]*mas[i]*mas[i]; 
cout « b « " "; 
} 
} 
return 0; 
}
